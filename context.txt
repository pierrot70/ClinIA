Voici un résumé prêt à coller dans un nouveau chat.
Contexte ClinIA (22 Feb 2026)
Objectif: masquer le toggle “IA réelle / IA mock” en production (Coolify) et verrouiller côté backend pour ignorer toute activation involontaire.

Frontend (Vite):
- Masquage en prod basé sur `import.meta.env.PROD` (pas sur `VITE_APP_ENV`, souvent absent sur Coolify).
- En prod: suppression de `clinia_force_real` du localStorage et `forceReal` forcé à `false`.
- Boutons/toggles et warning “IA réelle activée” masqués en prod.
Fichiers modifiés:
- frontend/src/components/Header.tsx
- frontend/src/pages/Results.tsx
- frontend/src/pages/ClinicalAnalyzePage.tsx

Backend:
- Verrouillage: en prod ou si `CLINIA_FORCE_MOCK=true`, on force `forceReal` à `false`.
- `useMock` et circuit breaker utilisent `forceRealSafe` (normalisé).
Fichier modifié:
- backend/index.js

Remarque:
- Tout est désormais bloqué en prod même si le client envoie `forceReal=true`.
- Option possible: ajouter un log explicite quand `forceReal` est reçu en prod.