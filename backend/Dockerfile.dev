FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY . .

EXPOSE 4000

# Nodemon est une devDependency verrouillée; aucune installation globale n'est requise.
CMD ["npx", "--no-install", "nodemon", "-L", "index.js"]
