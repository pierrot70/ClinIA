# Secrets and local configuration must never enter a Docker build context.
.env
.env.*
*.pem
*.key
cookies.txt

# Local/runtime artifacts are rebuilt inside the image or mounted for development.
node_modules
coverage
test-results
*.log
npm-debug.log*

# Source-control and editor metadata are not runtime dependencies.
.git
.gitignore
.vscode
.idea
