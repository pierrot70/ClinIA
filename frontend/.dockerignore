# Secrets and local configuration must never enter a Docker build context.
.env
.env.*
*.pem
*.key
cookies.txt

# Local/generated artifacts are rebuilt inside the image.
node_modules
dist
coverage
test-results
*.log
npm-debug.log*

# Source-control and editor metadata are not runtime dependencies.
.git
.gitignore
.vscode
.idea
